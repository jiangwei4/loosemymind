{% extends 'base.html.twig' %}

{% block title %}Hello ListePartieController!{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="example-wrapper">
 {% if listeEscapeGame|length < 1 %}
        <p>Aucune video</p>
    {% endif %}

    {% for escapeGame in listeEscapeGame %}
<br>

            <div class="w3-card-4" style="width:100%;">
                <header class="w3-container w3-blue">
                    <h1>{{ escapeGame.nom }}</h1>
                </header>

                <div class="w3-container" style="height:400px">
                    <!--<iframe  width="100%" height="100%" src="{{ escapeGame.nom }}?showinfo=0" frameborder="0" allowfullscreen></iframe>-->
                </div>
                <div class="w3-container">
                    <p>{{ escapeGame.description }}</p>
                </div>

        </div>



    {% endfor %}

</div>
{% endblock %}
