# loosemymind

## configurer wamp
virtual device 
creation de la bdd : escapegamebdd

## creation du projet
```
composer install 
php bin/console doctrine:schema:create 
```

## .env à configurer

## Lancer le serveur
```
php -S localhost:8000 -t public  
```